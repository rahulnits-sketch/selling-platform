# Backend

This folder contains a simple Express backend skeleton for the car dealer platform.

## Run backend

1. Install dependencies: `npm install`
2. Start server: `npm start`

The backend listens on `http://localhost:4000` by default.
